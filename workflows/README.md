# Workflow assets

Keep only the current workflow draft here.

## Active version
- `fastq_count_main_graph.md` — current main graph for MVP (`FASTQ` + `Count matrix`)

## Notes
- Older drafts are deleted once a newer one is accepted, so there is only one file to read.
- When the workflow changes again, overwrite the active file in place rather than adding a new filename.
