# Isolated from the scientific environment on purpose — see README.md.
# Install into a dedicated venv, never into dcode-scrna, and never edit
# environment.yml or conda-lock.yml for this service.
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pytest==8.3.4
httpx==0.28.1
